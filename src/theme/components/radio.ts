import type { ComponentStyle, RadioProps } from "@yamada-ui/react";

/**
 * ラジオボタンコンポーネントのスタイル定義
 */
export const Radio: ComponentStyle<RadioProps> = {
  defaultProps: {
    iconProps: {
      bg: "white",
    },
  },
};
