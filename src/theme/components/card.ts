import { type CardProps, type ComponentMultiStyle } from "@yamada-ui/react";

/**
 * Cardコンポーネントのスタイル定義
 */
export const Card: ComponentMultiStyle<CardProps> = {
  baseStyle: {
    container: {
      bgColor: "white",
    },
  },
};
