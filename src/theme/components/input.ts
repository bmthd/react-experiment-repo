import type { ComponentMultiStyle, InputProps } from "@yamada-ui/react";

/**
 * Inputコンポーネントのスタイル定義
 */
export const Input: ComponentMultiStyle<InputProps> = {
  defaultProps: {
    bg: "white",
  },
};
