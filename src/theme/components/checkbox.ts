import { type CheckboxProps, type ComponentStyle } from "@yamada-ui/react";

/**
 * チェックボックスコンポーネントのスタイル定義
 */
export const Checkbox: ComponentStyle<CheckboxProps> = {
  defaultProps: {
    iconProps: {
      bg: "white",
    },
  },
};
